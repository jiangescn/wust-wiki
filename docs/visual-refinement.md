# 视觉与交互调整（2026-09-24）

本轮从 [组件、动画与视觉研究](../research/component-motion-visual-audit-2026-09-24.md) 中选取范围小、可独立回退的项目。保留现有内容、15 个首页入口、路由、原版 Coder 卡片与博客正文组件；没有新增依赖，没有推送或部署。

## 实际改动

| 位置 | 行为 | 源码入口 |
| --- | --- | --- |
| 培养方案 | 640px 及以下将学院链接折叠为“学院定位”，默认收起；原生键盘操作、学院锚点强调和跳转留白；桌面仍平铺链接 | `app/components/content/CurriculumDirectory.vue` |
| 完整博客示例 | 1024px 及以下将目录默认折叠放在正文前，辅助侧栏放在正文及许可后；桌面仍是右侧粘性侧栏。两条示例路径共享实现，正文和交互插槽仅渲染一次 | `app/pages/blog-components.vue` |
| 空结果 | 新增共享 `DirectoryState`，直接复用 Nuxt UI 的 `UEmpty`、`UButton`，在培养方案和竞赛提供清除筛选操作；清除后焦点返回搜索框，培养层次保留 | `app/components/DirectoryState.vue`、上述培养方案组件、`app/components/content/CompetitionDirectory.vue` |
| 竞赛筛选 | 复用已有 `WikiMotion`，仅对结果区增加 160ms、0.9→1 的轻淡入；关闭外层高度追踪，保留原详情展开动画 | `app/components/content/CompetitionDirectory.vue` |
| 首页入口 | 增加清晰的键盘焦点描边；精细指针悬停上移 2px 并添加轻阴影，触摸端不增加悬停位移 | `app/app.css` |
| 校友博客排序 | 将图标点击入口改为带名称的原生按钮，支持 Enter、空格和可见焦点；排序逻辑及原卡片动画保持原样 | `app/components/content/WustBlogList.vue` |
| 减少动态效果 | 补足对原博客 `float-in-*` 的覆盖，并取消首页悬停位移，尊重 `prefers-reduced-motion` | `app/app.css` |

直接复用：Nuxt UI 空状态与按钮、已有 WikiMotion、原生 details、原版博客和 Coder 组件。必要适配：示例页面响应式布局、按钮语义及焦点衔接。自行实现：薄封装 DirectoryState、首页反馈及锚点样式。未改原组件快照或迁移一致性声明；迁移布局说明同步在 `vendor/blog-v3/README.md`。

## 验证

- 内容检查通过：30 篇文档、15 个首页入口、61 个内部链接。原版组件一致性检查通过：14 个 XUPT 组件。
- 首轮常规类型检查通过；后续共享生成目录曾出现自动导入声明缺失，开发预览同时报告 `_content_docs` 表不存在。为排除并发生成干扰，使用独立目录再次生成：109 条路由成功，随后对独立 Nuxt 类型配置运行 `vue-tsc -b --noEmit`，退出码 0。未清理或接管其他开发服务。
- 浏览器验证使用 `http://127.0.0.1:4319/` 的独立静态产物，检查 390px 手机和 1440px 桌面、深浅色。首页、培养方案、竞赛、两个完整示例路径及 Coder 两页均未出现整页横向溢出；本次静态预览未捕获控制台错误或警告。
- 培养方案：22 个本科学院、87 个专业；切换研究生显示 21 个学院、149 个专业。手机目录键盘展开、锚点焦点和约 90px 的落点留白通过。空结果清除后返回搜索框，保留培养层次。
- 竞赛：空结果清除后恢复 253 项、首批 20 项，焦点返回搜索框；键盘展开详情和“显示更多”至 40 项通过。
- 示例：手机默认折叠目录、目录锚点跳转、辅助侧栏在正文后、正文单实例通过；桌面双栏和粘性侧栏、原标签切换通过。校友博客排序支持 Enter 与空格，排序前后均保留 6 张卡片；首页焦点轮廓为 2px。
- 减少动态效果已通过源码层叠优先级复核，本轮未切换操作系统设置做实机验证。外部图片服务、视频播放、乐谱实际出声不属于本轮验证结果。构建仍有较大 chunk 和既有静态/动态混合导入提示。

独立验证脚本与日志位于 `.cache/visual-refinement-20260924-1/verify.mjs`、`verify.log`。脚本使用本机已安装 Nuxt CLI，将构建和类型产物隔离到该目录；不属于生产依赖。静态预览不会热更新，源码后续变动需要重新生成。

## 单独回退本轮改动

回退点位于 `.cache/visual-refinement-20260924-1/`，包含逐文件原字节、前后 SHA-256 清单和 `rollback.mjs`。它不依赖 Git HEAD，因此不会用旧提交覆盖本轮开始前已有的未提交工作。根 README 和正在并行修改的导航文件未纳入回退；vendor README 的并发新增说明已保留在回退基线中。

在项目根目录运行，只检查、不修改：

```powershell
node .cache/visual-refinement-20260924-1/rollback.mjs
```

确认检查通过后，需要回退时运行：

```powershell
node .cache/visual-refinement-20260924-1/rollback.mjs --apply
```

脚本先校验所有路径、备份和当前文件哈希。若任何文件在交付后又被编辑，会在写入前整体拒绝，应人工合并相应改动，不要强制覆盖。已有文件恢复原字节；本轮新增文件移动到该回退点的 `removed/` 中，不永久删除。7 项隔离演练通过，涵盖干运行、后续改动冲突、备份损坏、缺少哈希、路径越界、完整恢复与重复执行；没有对真实项目执行回退。

该回退点被 Git 忽略，仅在本机保留。保留它才能使用上述回退命令；不要将其视为已提交的长期备份。
